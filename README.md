# Personal site

Static HTML, no build step. Edit the `.html` files directly.

- Layout knobs (margins, column widths) live at the top of `static/style.css` as CSS variables.
- `static/profile.jpg` is the photo, displayed at 130px.
- Deployed with GitHub Pages from the `main` branch, root folder.
